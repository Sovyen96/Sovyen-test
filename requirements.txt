# requirements.txt
# ─────────────────────────────────────────────────────────────
# Asesor Deportivo (app web de fútbol) — dependencias de Python
# Probado con Python 3.11+
# ─────────────────────────────────────────────────────────────

# App web
Flask>=3.0.0

# Cliente HTTP para football-data.org
requests>=2.31.0

# Variables de entorno desde archivo .env (desarrollo)
python-dotenv>=1.0.0

# El motor de probabilidad (Poisson + Dixon-Coles) usa solo la librería
# estándar (math), así que no requiere numpy ni scipy.
